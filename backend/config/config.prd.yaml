smtp:
  host: smtp.example.com
  port: "587"
  user: user@example.com
  pass: replace-me
  from: notify@example.com

auth:
  code_pepper: change-me
  token_secret: change-me

database_url: postgres://xledger:xledger_secret@127.0.0.1:5432/xledger?sslmode=disable
redis_url: redis://127.0.0.1:6379/0

gin_mode: release
enable_dev_login: true
n8n_base_url: http://127.0.0.1:5678

api_addr: ":8080"

# trusted_proxies: "127.0.0.1,::1"

google_auth:
  client_id: "replace-me"
  client_secret: "replace-me"
  redirect_url: https://xledger.xanne.cloud/api/auth/google/callback
  frontend_return: https://xledger.xanne.cloud/auth/google/callback
